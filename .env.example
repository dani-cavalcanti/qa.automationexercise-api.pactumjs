# URL base da API sob teste. Copie este arquivo para ".env" para sobrescrever o padrão.
BASE_URL=https://serverest.dev

# Tempo máximo (ms) aguardado por resposta antes de falhar o teste por timeout.
REQUEST_TIMEOUT=10000
